
server {
        listen 80;

# DOMAIN START
        server_name {{domain}} www.{{domain}};
# DOMAIN END

        root /opt/latent/{{site}};

        index index.html index.htm index.nginx-debian.html;

        location / {
            proxy_pass   http://127.0.0.1:{{port}}/;
        }
}
