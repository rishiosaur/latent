[Unit] 
Description={{site}} — {{domain}}
After=network.target 
Wants=nginx.service 
ConditionPathIsDirectory=/opt/latent/{{site}}


[Service]
WorkingDirectory=/opt/latent/{{site}}
Environment=PORT={{port}}
ExecStart=bash /opt/latent/{{site}}/entry.sh 
Restart=on-failure
RestartSec=5 
ExecReload=/bin/kill -HUP $MAINPID 
KillMode=process
Restart=on-failure 
Type=simple 

[Install] 
WantedBy=default.target